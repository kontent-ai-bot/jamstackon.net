<!DOCTYPE html>
<html lang="en">
<head>
    <title>TODO TITLE with SEO</title>
    <link rel="stylesheet" href="@IExecutionContext.Current.GetLink("/assets/styles/main.css")" />
    <link rel="stylesheet" href="@IExecutionContext.Current.GetLink("/assets/styles/prism.css")" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class="dark">
    <header>
        <div class="logo">
            <div class="title">@Html.Raw(ViewBag.HeaderTitle.Replace(".", "<span class='orange'>.</span>"))</div>
        </div>
        <nav>
            <ul>
                @{
                    foreach (var link in (IEnumerable<Link>)ViewBag.Links)
                    {
                        <li><a href="@link.Url">@link.Label</a></li>
                    }
                }

                @if (ViewBag.ShowDarkModeToggle)
                {
                    <li>
                        <label class="switch">
                            <input type="checkbox" checked="checked" onchange="this.checked ? document.querySelector('body').classList.add('dark') : document.querySelector('body').classList.remove('dark')">
                            <span class="slider round"></span>
                        </label>
                    </li>
                }
            </ul>
        </nav>
    </header>

    <main>
        <div class="layout">
            @RenderBody()
        </div>
    </main>
    <footer>
        <div class="footer-wrapper">
            <div class="logo">
                <div>@Html.Raw("Jamstack on .NET".Replace(".", "<span class='orange'>.</span>"))</div>
            </div>
            <div class="footer-column-group">
                <div class="footer-column">
                    <span>Resources</span>
                    <ul>
                        <li><a href="#">Resource 1</a></li>
                        <li><a href="#">Resource 2</a></li>
                        <li><a href="#">Resource 3</a></li>
                    </ul>
                </div>
                <div class="footer-column">
                    <span>Resources</span>
                    <ul>
                        <li><a href="#">Resource 1</a></li>
                        <li><a href="#">Resource 2</a></li>
                        <li><a href="#">Resource 3</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </footer>
    <script src="@IExecutionContext.Current.GetLink("/assets/scripts/prism.js")"></script>

</body>
</html>